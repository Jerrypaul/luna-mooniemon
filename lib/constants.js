const DEFAULT_WEIGHTS = {
  Common: 55,
  Uncommon: 25,
  Rare: 12,
  Epic: 6,
  Legendary: 2
};

const DEFAULT_PULL_COOLDOWN_MS = 4 * 60 * 60 * 1000;

module.exports = {
  DEFAULT_WEIGHTS,
  DEFAULT_PULL_COOLDOWN_MS
};
